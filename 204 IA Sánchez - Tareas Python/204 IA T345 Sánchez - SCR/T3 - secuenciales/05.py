import os
os.system ("cls")

gigabytes = int (input("gygabytes = "))

megabytes = gigabytes * 1024
kilobytes = megabytes * 1024
bytes = kilobytes * 1024

print(f"bytes = {bytes}")
print(f"kylobytes = {kilobytes}")
print(f"megabytes = {megabytes}")